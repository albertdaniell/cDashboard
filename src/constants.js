const MY_PROXY = `https://kecstock.jsi.com/api/`;
const FETCH_OPTIONS = {
  headers: {
    Authorization: `Basic ${btoa('albertagoya@gmail.com:Pa$$word1')}`
  }
}

export default {MY_PROXY,FETCH_OPTIONS}